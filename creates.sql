create table employee(
    id integer,
    nome varchar(255),
    sex char(1),
    status boolean
);

create table alunos(
    ra integer,
    nome varchar(50),
    sexo char(1),
    idade integer,
);
